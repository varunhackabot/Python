METHOD 1:

string = 'hello'
print(string[::-1])

METHOD 2:

a = ''
for i in range(len(string)-1,-1,-1):
  a+=string[i]
print(a)

METHOD 3:

a = [string[i] for i in range(len(string)-1,-1,-1)]
print(a)

METHOD 4:

a = (lambda x:x[::-1])
print(a(string))
